let doc* = """
Usage:
  touch [options] <file>...

Options:
  -a --access          Change only the access time
  -m --modify          Change only the modification time
  -c --no-create       Do not create any files
  -d --date <date>     Use the specified date (YYYY-MM-DD)
  -t --time <time>     Use the specified time (HH:MM)
  -r --reference <ref> Use the timestamp of the reference file
  -h --help            Show this help message
"""
